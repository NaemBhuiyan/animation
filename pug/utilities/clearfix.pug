extends ../_skeleton-component
include mixins/_clearfix

block append variables
  - var firstParent = 'utilities'
  - var currentPage = 'clearfix'

block component
  .card.mb-3.shadow-sm
    .card-body
      +PageHeader(pageHeader)
   
  .row
    .col-12
      .card.mb-3.shadow-sm
        .card-body
          p Easily clear floats by adding .clearfix to the parent element. Can also be used as a mixin.
          +componentCardLib
            .clearfix ...
          p.mt-4 The following example shows how the clearfix can be used. Without the clearfix the wrapping div would not span around the buttons which would cause a broken layout.

          +componentCard
            +clearfix

