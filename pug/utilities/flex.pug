extends ../_skeleton-component
include mixins/_flex


block append variables
  - var firstParent = 'utilities'
  - var currentPage = 'flex'

block component
  .card.mb-3.shadow-sm
    .card-body
      +PageHeader(pageHeader)
    
  
  .card.mb-3.shadow-sm
    .card-header
      h5.mb-1 Enable flex behaviors
      p.mb-0 Apply 
        code display
        |  utilities to create a flexbox container and transform direct children elements into flex items. Flex containers and items are able to be modified further with additional flex properties.
    .card-body.bg-light
      +componentCard
        +flexBasic

  .card.mb-3.shadow-sm
    .card-header
      h5.mb-1 Direction
      p.mb-0 Use 
        code .flex-row 
        | to set a horizontal direction (the browser default), or .flex-row-reverse to start the horizontal direction from the opposite side.

    .card-body.bg-light
      +componentCard
        +flexDirection.flex-row
        +flexDirection.flex-row-reverse
      p.mt-3 Use 
        code .flex-column 
        | to set a vertical direction, or 
        code .flex-column-reverse 
        | to start the vertical direction from the opposite side
      +componentCard
        +flexDirection.flex-column
        +flexDirection.flex-column-reverse

        
    
  .card.mb-3.shadow-sm
    .card-header
      h5.mb-1 Justify Content
      p.mb-0 Use 
        code justify-content 
        | utilities on flexbox containers to change the alignment of flex items on the main axis (the x-axis to start, y-axis if 
        code flex-direction: column
        | ). Choose from start (browser default), 
        code end
        | , 
        code center
        | , 
        code between
        | , or 
        code around.
    .card-body.bg-light
      +componentCard
        +flexJustifyContent
  .card.mb-3.shadow-sm
    .card-header
      h5.mb-1 Align items
      p Use 
       code align-items 
       | utilities on flexbox containers to change the alignment of flex items on the cross axis (the y-axis to start, x-axis if 
       code flex-direction: column
       | ). Choose from 
       code start
       | , 
       code end
       | , 
       code center
       | , 
       code baseline
       | , or 
       code stretch (browser default).
    .card-body.bg-light
      +componentCard
        +flexAlignItem

  .card.mb-3.shadow-sm
    .card-header
      h5.mb-1 Align self
      p.mb-0 Use 
        code align-self 
        | utilities on flexbox items to individually change their alignment on the cross axis (the y-axis to start, x-axis if 
        code flex-direction: column
        | ). Choose from the same options as 
        code align-items: start, 
        code end
        | , 
        code center
        | , 
        code baseline
        | , or 
        code stretch (browser default).
    .card-body.bg-light
      +componentCard
        +flexAlignSelf("start").align-self-start
        +flexAlignSelf("end").align-self-end
        +flexAlignSelf("center").align-self-center
        +flexAlignSelf("baseline").align-self-baseline
        +flexAlignSelf("stretch").align-self-stretch
        

  .card.mb-3.shadow-sm
    .card-header 
      h5.mb-1 Fill
      p.mb-0 Use the 
        code .flex-fill 
        | class on a series of sibling elements to force them into widths equal to their content (or equal widths if their content does not surpass their border-boxes) while taking up all available horizontal space.
    .card-body.bg-light
      +componentCard
        +flexFill
  .card.mb-3.shadow-sm
    .card-header
      h5.mb-1 Grow and shrink
      p.mb-0 Use 
        code .flex-grow-* 
        | utilities to toggle a flex item’s ability to grow to fill available space. In the example below, the 
        code .flex-grow-1 
        | elements uses all available space it can, while allowing the remaining two flex items their necessary space.

    .card-body.bg-light
      +componentCard
        +flexShrink
  .card.mb-3.shadow-sm
    .card-header 
      h5.mb-1 Auto margins
      p.mb-0 Flexbox can do some pretty awesome things when you mix flex alignments with auto margins. Shown below are three examples of controlling flex items via auto margins: default (no auto margin), pushing two items to the right (
          code .mr-auto
          | ), and pushing two items to the left (
          code .ml-auto
          | ).
    .card-body.bg-light
      +componentCard
        +flexMargin
  .card.mb-3.shadow-sm
    .card-header
      h5.mb-1 With align items
      p.mb-0 Vertically move one flex item to the top or bottom of a container by mixing 
        code align-items
        | , 
        code flex-direction: column 
        | , and 
        code margin-top: auto or margin-bottom: auto.
    .card-body.bg-light
      +componentCard
        +flexWithAlignItems.align-items-start
        +flexWithAlignItems.align-items-end
  .card.mb-3.shadow-sm
    .card-header
      h5.mb-1 Wrap
      p.mb-0 Change how flex items wrap in a flex container. Choose from no wrapping at all (the browser default) with 
        code .flex-nowrap
        | , wrapping with 
        code .flex-wrap
        | , or reverse wrapping with 
        code .flex-wrap-reverse
        | .
    .card-body.bg-light
      +componentCard
        +flexNoWrap
        +flexWrap.flex-wrap
        +flexWrap.flex-wrap-reverse
        
  .card.mb-3.shadow-sm
    .card-header
      h5.mb-1 Order
      p.mb-0 Change the 
        em visual 
        | order of specific flex items with a handful of 
        code order 
        | utilities. We only provide options for making an item first or last, as well as a reset to use the DOM order. As 
        code order 
        | takes any integer value (e.g., 
        code 5
        | ), add custom CSS for any additional values needed.
    .card-body.bg-light
      +componentCard
        +flexOrder
  .card.mb-3.shadow-sm
    .card-header
      h5.mb-1 Align content
      p.mb-0 Use 
        code align-content 
        | utilities on flexbox containers to align flex items 
        em together 
        | on the cross axis. Choose from 
        code start (browser default)
        | , 
        code end
        | , 
        code center
        | , 
        code between
        | , 
        code around
        | , or 
        code stretch. To demonstrate these utilities, we’ve enforced 
        code flex-wrap: wrap 
        | and increased the number of flex items.
    .card-body.bg-light
      +componentCard
        +flexAlignContent.align-content-start
        +flexAlignContent.align-content-center
        +flexAlignContent.align-content-end
        +flexAlignContent.align-content-between
        +flexAlignContent.align-content-around
        +flexAlignContent.align-content-stretch
        

  
  